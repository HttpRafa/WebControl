export class SimpleApplication {

    name: string
    id: number
    icon: number

    constructor(name: string, id: number, icon: number) {
        this.name = name;
        this.id = id;
        this.icon = icon;
    }

}